/**
 *  @file player.ser
 *  @version Act_1_0
 */


#ifndef PLAYER_SER
#define PLAYER_SER

#include "datastreamcursor.h"
#include "stringlistcursor.h"

#include "Entities/player.h"
#include "Logging/logcore.h"

#include "context.ser"


namespace abi {

/**
 * @brief The entity_traits class
 * byte and words measuring traits for Entity
 */
template<>
struct entity_traits<Player, abi::Version::Act_1_0> {
    static constexpr bool is_fixed = false;
    static constexpr int minimum_bytes = sizeof(qsizetype)*2;
    static constexpr int maximum_bytes = minimum_bytes;

    static constexpr const char* name = "Player";
    static constexpr int minimum_words = 3;
    static constexpr int maximum_words = minimum_words;
};

/**
 * @brief The Writer class
 * version generalized write interface
 */
template<>
struct Writer<Player, abi::Version::Act_1_0> {
    static void write(DataStreamCursor& out, [[gnu::unused]] const Player& c = Player::instance()) {
        abi::write<Context, abi::Version::Act_1_0>(out, Player::getExperience());
    }

    static void write(StringListCursor& out, [[gnu::unused]] const Player& c = Player::instance()) {
        out.append(entity_traits<Player, abi::Version::Act_1_0>::name);
        abi::write<Context, abi::Version::Act_1_0>(out, Player::getExperience());
    }
};

/**
 * @brief The Reader class
 * version generalized read interface
 */
template<>
struct Reader<Player, abi::Version::Act_1_0> {
    static void read(DataStreamCursor& in, [[gnu::unused]] Player& c = Player::instance()) {
        abi::read<Context, abi::Version::Act_1_0>(in, Player::getExperience());
    }

    static void read(StringListCursor& in, [[gnu::unused]] Player& c = Player::instance()) {
        if(in.remaining() < entity_traits<Context, abi::Version::Act_1_0>::minimum_words){
            qWarning(logCore) << "Reader<Player, Atc_1_0>::read: not enought words to parse";
            return;
        }

        in.skip();  // skip typename

        abi::read<Context, abi::Version::Act_1_0>(in, Player::getExperience());
    }
};

}   // namespace abi

#endif // PLAYER_SER
